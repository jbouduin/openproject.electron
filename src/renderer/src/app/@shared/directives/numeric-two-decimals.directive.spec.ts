import { NumericTwoDecimalsDirective } from './numeric-two-decimals.directive';

describe('NumericTwoDecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericTwoDecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
